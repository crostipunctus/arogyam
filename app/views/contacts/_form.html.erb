<script src="https://www.google.com/recaptcha/api.js?render=<%= Rails.application.credentials.recaptcha[:site_key]%>"></script>
<%= form_with model: @contact,
              local: true,
              id: "contact_form",
              data: {
                controller: "recaptcha-form",
                action: "submit->recaptcha-form#submit",
                recaptcha_form_site_key_value: Rails.application.credentials.recaptcha[:site_key],
                recaptcha_form_action_name_value: "contact"
              } do |f| %>
  <%= render "shared/errors", object: @contact %>
  <div class="mb-3">
    <%= f.label :name, class: "form-label fw-medium" %>
    <%= f.text_field :name, class: "form-control", placeholder: "Your name" %>
  </div>
  <div class="mb-3">
    <%= f.label :email, class: "form-label fw-medium" %>
    <%= f.text_field :email, class: "form-control", placeholder: "Your email address" %>
  </div>
  <div class="mb-4">
    <%= f.label :message, class: "form-label fw-medium" %>
    <%= f.text_area :message, class: "form-control", rows: 5, placeholder: "How can we help you?" %>
  </div>
  <div>
    <%= f.submit 'Send Message', class: 'btn btn-primary rounded-pill px-4' %>
  </div>
<% end %>
