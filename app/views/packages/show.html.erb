<div class="overflow-hidden">
  <div class="container pt-5 pb-sm-3 mt-5 mb-2 mb-md-3 mb-lg-4 mb-xxl-5">
    <%= link_to "<   Back to all packages", packages_path, class: "nav-link", id: "back_to", class: "explorebutton btn btn-outline-primary rounded-0" %>
    <h1 id="packageName" class="text-center pb-3 pb-md-4 pb-lg-5 mb-lg-0 mb-xl-2 mb-xxl-4"><%= @package.name %></h1>
    
    <article class="row align-items-top card-hover pb-5 mb-md-2 mb-lg-3 mb-xl-4">
      <div class="col-md-5 offset-xxl-1 mb-4 ">
        <%= package_image(@package) %></div>
      <div class="col-md-5 col-xl-4">
        <div class="ms-md-2 ms-lg-5 ms-xxl-0" style="max-width: 620px;">
          <span id="package" class="package mb-0 mb-md-1 mb-lg-3"><%= @package.content%></span><br>
          <div class="package-titles"><strong>Eligibility:</strong></div>
          <span id="package" class="mb-0 mb-md-1 mb-lg-3"><%= @package.eligibility%></span><br>
          <div class="package-titles"><strong>Note:</strong></div>
          <span id="package" class="mb-0 mb-md-1 mb-lg-3"><%= @package.note%></span>
          
          <div class="package-titles"><strong>Duration:</strong></div>
          <h2 class="mb-lg-4"><%= "#{@package.duration} days" %></a></h2>

          <div class="package-titles"><strong>Dates:</strong></div>
          <h2 class="mb-lg-4"><%= "#{@package.dates}" %></h2>

          <div class="package-titles"><strong>Cost:</strong></div>
          <h2 class="mb-lg-4"><%= cost(@package.cost) %></a></h2>
          
          <% if current_user_admin? %>
            <div class="admin">
              <%= link_to "Edit package", edit_package_path(@package), class: "btn btn-primary btn-sm" %>
            </div>
            <div  class="admin">
              <%= link_to "Delete package", destroy_package_path(@package), class: "btn btn-danger btn-sm", data: {turbo_method: :delete, turbo_confirm: "Are you sure?" }%>
            </div>
          <% end %>
          
        </div>
      </div>
    </article>
    
</div>

