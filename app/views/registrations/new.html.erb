<section class="container py-5 mt-5 mb-md-3 mb-lg-4 mb-xxl-5">

<div class="Profileform">
  <div class="h-100 d-flex align-items-center justify-content-center">
     <div class="col-12 col-sm-10 col-md-8 col-lg-6">
    <%= form_with(model: @registration) do |f|%>
      <%= render 'shared/errors', object: @registration %> 
     <%= f.hidden_field :batch_id %>
       <div class="mb-3">
          <%= f.label :package_id, "Programme *" %>
          <%= f.collection_select :package_id, Package.all, :id, :name, { prompt: "Select a package" }, class: "form-control" %>
        </div>
        <div class="mb-3">
          <%= f.label :subtances, "Do you currently or in the past have used tobacco, alcohol, recreational drugs, or illicit substances? If yes, please list substance and frequency of use: *" %>
          <%= f.text_area :substances, class: "form-control" %>
        </div>
        <div class="mb-3">
          <%= f.label :lifestyle, "Please describe your lifestyle in brief: *" %>
          <%= f.text_area :lifestyle, class: "form-control" %>
        </div>
        <div class="mb-3">
          <%= f.label :health_conditions, "Please describe all your health conditions in detail.  Please note: We need this information to be able to guide you better. *" %>
          <%= f.text_area :health_conditions, class: "form-control" %>
        </div>
        <div class="mb-3">
          <%= f.label :medication, "Are you currently on any regular Allopathic/Ayurvedic/Homeopathic or any other medicine? If yes please give us details  *" %>
          <%= f.text_area :medication, class: "form-control" %>
        </div>

      
      
      <%= f.submit 'Register', class:"mt-3 btn btn-outline-success rounded-pill"%>
    <% end %>
    </div>
  </div>
</div>
</section>